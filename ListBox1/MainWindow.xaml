<Window x:Class="ListBox1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListBox1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
            <TextBlock Text="Nombre" Margin="10"/>
            <TextBox Name="txtNombre" Margin="10" Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0">
            <TextBlock Text="Apellidos" Margin="10"/>
            <TextBox Name="txtApellidos" Margin="10" Width="200"/>
        </StackPanel>

        <Button Name="tbnAnadir" Content="Añadir"
                Grid.Row="0" Grid.Column="1"
                Margin="10" Width="100"
                Click="tbnAnadir_Click"
                IsDefault="True"/>
        <ListBox Name="listNombres" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                 Margin="10"
                 SelectionChanged="listNombres_SelectionChanged"/>
        <Label Name="labelSalida"
               Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
               Margin="10"/>
    </Grid>
</Window>
